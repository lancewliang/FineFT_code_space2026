from common.metric_columns import MetricColumns


def test_metric_columns_literals() -> None:
    assert MetricColumns.TR == "tr"
    assert MetricColumns.PORTFOLIO_TR == "portfolio_tr"
    assert MetricColumns.MDD == "mdd"
    assert MetricColumns.ANNUAL_SR == "annual_sr"
    assert MetricColumns.DAILY_CR == "daily_cr"
    assert MetricColumns.DAILY_VOL == "daily_vol"
    assert MetricColumns.DAILY_SOR == "daily_SoR"
    assert MetricColumns.DOWNSIDE_DEVIATION_DAILY == "downside_deviation_daily"
    assert MetricColumns.REQUIRED_MONEY == "required_money"
    assert MetricColumns.TRIAL_ID == "trial_id"
    assert MetricColumns.NUM_CONTRACTS == "num_contracts"
    assert MetricColumns.CONTRACT == "contract"
    assert MetricColumns.DF_PATH == "df_path"
    assert MetricColumns.DF_LENGTH == "df_length"
    assert MetricColumns.REWARD_SUM == "reward_sum"
    assert MetricColumns.REWARD_MAX == "reward_max"
    assert MetricColumns.NORMALIZED_REWARD == "normalized_reward"
    assert MetricColumns.TRANS_REWARD_MEAN == "trans_reward_mean"
    assert MetricColumns.TRANS_REWARD_STD == "trans_reward_std"
    assert MetricColumns.TURNOVER == "turnover"
    assert MetricColumns.MEAN_TURNOVER == "mean_turnover"
    assert MetricColumns.MEAN_POSITION == "mean_position"
    assert MetricColumns.MEAN_ABS_POSITION == "mean_abs_position"
    assert MetricColumns.LONG_STEP_RATIO == "long_step_ratio"
    assert MetricColumns.SHORT_STEP_RATIO == "short_step_ratio"
    assert MetricColumns.FLAT_STEP_RATIO == "flat_step_ratio"
    assert MetricColumns.RETURN_RATE == "return_rate"
    assert MetricColumns.SCORE == "score"
    assert MetricColumns.REQUIRE_MONEY == "require_money"
    assert MetricColumns.SOURCE_FILE == "source_file"
    assert MetricColumns.ROWS == "rows"
    assert MetricColumns.INDICATOR == "indicator"
