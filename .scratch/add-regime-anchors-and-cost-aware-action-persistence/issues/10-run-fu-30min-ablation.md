# 10: 运行 fu/30min 全量消融并决定是否启用

**What to build:** 在真实 `fu/30min` 数据上串行运行三组消融，依据目标四格收益、换仓成本、全 valid 和高波动格非回归条件，决定增强配置是否可成为新实验默认值。

**Blocked by:** 09: 建立三组消融与 16 格评估流水线。

**Status:** ready-for-agent

- [ ] 使用 `conda activate finetf`，固定训练预算、种子、epoch/checkpoint 候选和费用配置完成 A/B/C。
- [ ] 输出全部 16 格、目标四格、全 valid 和高波动格的最终对照报告。
- [ ] 只有 C 相对 A 的目标四格宏观净收益改善、至少三个目标格不下降、换仓及实际手续费+滑点下降，且没有超出预先声明容忍度的全局退化时，才启用增强配置。
- [ ] 若门槛未通过，保留实现和诊断能力，但维持默认关闭，并记录失败原因。
- [ ] 不修改 test 数据、现有基线模型或当前选中特征 Manifest。

