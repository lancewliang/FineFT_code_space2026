## 1. Implementation

- [x] 1.1 Add focused tests for feature selection JSON dataclass objects covering object return types, object attribute access, and `to_dict()` equality with written JSON files. <!-- 已实现: focused tests now require result dataclasses, object attribute access, and to_dict JSON compatibility; RED verified with missing manifests module -->
- [x] 1.2 Add `data_preprocess/operator_futures/feature_selection/manifests.py` with dataclass models for feature selection manifests, feature union manifests, feature score windows, and result return objects. <!-- 已实现: added feature selection JSON dataclasses, write_json helpers, and result return objects; py_compile passed -->
- [x] 1.3 Refactor `data_preprocess/operator_futures/feature_selection/muti_contract/pipeline.py` to use `FeatureSelectionManifest` and return `FeatureSelectionResult` while preserving `feature_selection_manifest.json`. <!-- 已实现: pipeline train/valid manifests now use FeatureSelectionManifest and return FeatureSelectionResult; focused tests passed -->
- [x] 1.4 Refactor `data_preprocess/operator_futures/feature_selection/contract_feature_union.py` to use `FeatureUnionManifest` and return `FeatureUnionResult` while preserving `feature_union_manifest.json`. <!-- 已实现: feature union manifest now uses FeatureUnionManifest, typed output shapes, and FeatureUnionResult; focused tests passed -->
- [x] 1.5 Refactor `data_preprocess/operator_futures/feature_selection/ic_correlation.py` and `rank_ic_correlation.py` to use `FeatureScoreWindow` and return result objects while preserving window score JSON. <!-- 已实现: IC and RankIC now return result dataclasses and persist FeatureScoreWindow JSON with top-level feature-score mappings -->
- [x] 1.6 Run focused verification for feature selection tests, Python compilation, and OpenSpec validation. <!-- 已实现: focused tests, py_compile, and openspec strict validation all passed -->
