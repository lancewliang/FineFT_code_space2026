## 1. Implementation

- [x] 1.1 Update focused tests to require dataclass interfaces for loss NaN diagnostics, qtable diagnostics, and parallel rollout contracts while preserving legacy `.to_dict()` and file-format assertions. <!-- 已实现: focused tests now assert dataclass contracts and RED failures for missing dataclass types -->
- [x] 1.2 Refactor `loss_nan_diagnostics.py` to return dataclass diagnostics and update logging to use attributes. <!-- 已实现: loss NaN summaries, locations, and diagnostics now use dataclass objects with legacy-compatible to_dict logging -->
- [x] 1.3 Refactor `pretrain_qtable_diagnostics.py` to use dataclass sample items, manifest, CSV rows, sample diagnostics, worker result, and prepare result while preserving manifest JSON and diagnostics CSV formats. <!-- 已实现: qtable diagnostics now returns dataclass plan/result/diagnostics objects while preserving manifest JSON, CSV output, cache reuse, and legacy tuple-key cache lookup -->
- [x] 1.4 Update `weight_advantage_pretrain.py` and the qtable-related portions of `parallel_weight_advantage_pretrain.py` to consume `PretrainQTableDiagnosticsResult` and `SamplePlanItem` via attributes. <!-- 已实现: serial and parallel qtable callers now consume PretrainQTableDiagnosticsResult attributes; serial sample loop uses SamplePlanItem -->
- [x] 1.5 Refactor `parallel_weight_advantage_pretrain.py` rollout task, epoch params, worker messages/results/errors, metrics, transition records, and round summaries to dataclass objects. <!-- 已实现: parallel rollout task, epoch params, worker payload/result/error, transitions, metrics, and summaries now use dataclass objects -->
- [x] 1.6 Run focused tests, Python compilation, and OpenSpec strict validation. <!-- 已实现: focused tests, py_compile, OpenSpec strict validation, and stale business-record scan passed -->
